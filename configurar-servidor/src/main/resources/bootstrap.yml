server:
  port: 8082

spring:
  application:
    name: configurar-servidor
  cloud:
    config:
      server:
        git:
          uri: https://github.com/FRNalexander/Microservicios.git
          searchPaths: configurar-datos
          username: ${USER_GIT}
          password: ${PASSWORD_GIT}

  security:
    user:
      name: root
      password: s3cr3t
